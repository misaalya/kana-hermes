# Development bootstrap access password (login page is enabled when set).
# NEVER commit a real password here — git history keeps it forever. Copy
# .env.example for the full variable list and keep real values in untracked
# files only.
KANA_ACCESS_PASSWORD=test
