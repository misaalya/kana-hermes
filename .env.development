# Dev bootstrap access password (login page is enabled when set).
# Change it in the app after first login; that bcrypt hash then wins.
KANA_ACCESS_PASSWORD=test

# Default cloned voice for Qwen3-TTS (Base model speaks through clones).
KANA_TTS_DEFAULT_VOICE=clone-b2176303f2264f8ba3fdbd3a375d66ef
