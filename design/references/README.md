# Design references

Product-owner-provided visual references (mobile game UI screenshots) used
only as direction for Kana's original CSS theme. They are intentionally kept
outside `public/` and outside the npm package whitelist: Kana must never
redistribute third-party copyrighted artwork or ship it inside its bundle.
