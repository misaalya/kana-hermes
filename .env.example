# Mock providers are visible only in explicit development builds.
NEXT_PUBLIC_KANA_DEVELOPMENT_MODE=true

# The packaged `kana` launcher sets this server-only flag at runtime.
KANA_LOCAL_RUNTIME_CONTROL=0
